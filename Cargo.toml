[workspace]
members = [
    "netrender_device",
    "netrender",
    "webrender_api",
    "wr_malloc_size_of",
    "peek-poke",
]
resolver = "2"

[profile.release]
debug = true
panic = "abort"

[profile.dev]
panic = "abort"
